name: card_info
description: Card Info app

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^7.0.0
  intl: ^0.17.0
  dartz: ^0.10.1
  get_it: ^7.2.0
  equatable: ^2.0.2
  animate_do: ^2.0.0
  injectable: ^1.5.0
  auto_route: ^3.2.0
  google_fonts: ^2.1.0
  flutter_bloc: ^7.0.1
  freezed_annotation: ^0.14.3
  easy_localization: ^3.0.0

dev_dependencies:
  freezed: ^0.14.5
  build_runner: ^2.1.4
  flutter_lints: ^1.0.3
  injectable_generator: ^1.5.2
  auto_route_generator: ^3.2.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/translations/
